-content_for :title, @step_name

.col-md-8
  %h5 Step #{@step_n}
  %h1= yield(:title)
  = render partial: "step/steps/#{@step}"

  .text-right
    =link_to 'Continue', step_path(@next), class: 'btn btn-primary'
